var autoSequelize = require('./index');
var auto = new autoSequelize('sequelize_test', 'daniel', null, {dialect: 'postgres', port: 5432, omitNull: true});
// var auto = new autoSequelize('sequelize_test', 'root', null, {dialect: 'mysql', omitNull: true});

auto.run({spaces: true, indentation: 2}, function(err, results){
  // var cartItems = auto.sequelize.import(__dirname + '/models/cartItems');
  // cartItems.create({qty: 5, item: 'test'})
  // .success(function(item){
    // console.log('item', item);
  // });
});