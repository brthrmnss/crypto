var mega = require('../lib/mega')

var email = 'brthrmnss@hotmail.com'
var password = '44444! #MiJY'
var name = 'newfolder'
var target = null; //-1// 'newfolder'

var storage = mega({email: email, password: password, keepalive: false})

storage.mkdir({
    name: name,
    target: target
  },
  function(err, file) {
    if (err) throw err
    console.log('\nCreated', file.name)
  }
)