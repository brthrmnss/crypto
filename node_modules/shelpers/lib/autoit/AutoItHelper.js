/**
 * Created by user on 7/12/15.
 */
var sh = require('shelpers').shelpers;

/**
 * Requirements
 * Ruby 1.9?
 * Autoit Installation
 * @constructor
 */

function AutoItHelper() {
    var p = AutoItHelper.prototype;
    p = this;
    var self = this;
    p.init = function init(url, appCode) {
        //make CommandRunner
        //send in commands using newlines
        //store output in array , feed output of commands like clipboard
        //to node variable

        //?what does this give us?
    }

    p.proc = function debugLogger() {
        if ( self.silent == true) {
            return
        }
        sh.sLog(arguments)
    }


}

exports.AutoItHelper = AutoItHelper;

if (module.parent == null) {

    var a = new AutoItHelper();
    a.init();
}



